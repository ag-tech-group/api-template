# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/api_template

# Auth - generate with: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-in-production

# Environment
ENVIRONMENT=development

# CORS — comma-separated origins for production, e.g. "https://myapp.com,https://www.myapp.com"
CORS_ORIGINS=

# Frontend URL
FRONTEND_URL=http://localhost:5173

# Cookie domain (leave empty for localhost development)
COOKIE_DOMAIN=
